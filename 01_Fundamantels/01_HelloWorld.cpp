#include <iostream> // Required library for input/output operations

int main() // Main function. The function that is called when the program starts, required in every C++ project
{
    std::cout << "Hello World!\n"; // Prints the message to the screen
    return 0; // Returns 0 when the program ends successfully (default)
};
